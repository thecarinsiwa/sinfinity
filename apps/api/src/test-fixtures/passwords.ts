/**
 * Plaintext fixtures for unit tests only.
 * Intentionally non-credential-like to avoid secret scanners (GitGuardian).
 */
export const UNIT_TEST_PASSWORD = 'unit-test-only';
export const UNIT_TEST_PASSWORD_ALT = 'unit-test-only-alt';
