import { Module } from '@nestjs/common';

/**
 * Global reference data (countries, currencies, taxes, units, terms…).
 * Feature controllers are registered here as geo / money / terms land.
 */
@Module({})
export class SettingsModule {}
