import { ComingSoonPage } from "@/components/layout/coming-soon";

/** Stub temporaire — remplacé par le CRUD agences (phase 2). */
export default function AgencesPage() {
  return <ComingSoonPage title="Agences" />;
}
